---
cf_id: CF-0118
title: "adk web silently degrades — dev chat UI unreachable but server starts with WARNING only"
category: Dev Server Startup
affected_system: Google ADK (adk web/dev_server)
layer: Installation→Server Startup
failure_mode: "C3 假信信号 — server reports startup, /dev-ui/ returns 404; WARNING buried in logs"
trigger: "Fresh pip install google-adk following official docs, then adk web"
observed_symptom: "Server starts, GET / → 307 → /dev-ui/, but /dev-ui/ 404s; DevServer silently not mounted"
root_cause: "Undeclared transitive deps (google-cloud-storage, python-multipart) cause DevServer import failure; fallback to ApiServer without dev UI"
detection: "Indirect: browser shows 404 on chat UI; server log WARNING easy to miss"
impact: "New users follow docs, get broken dev experience; no error message surfacing the missing deps"
mitigation: "Manually install google-cloud-storage + python-multipart"
reference: 
issue: https://github.com/google/adk-python/issues/5787
status: Open
tier: B
taxonomy: configuration
old_cf_id: 
pr: 
evidence_level: reproduced
date_added: 2026-07-21
---
# CF-0118: adk web silently degrades — dev chat UI unreachable but server starts with WARNING only

## Source
- **Issue:** https://github.com/google/adk-python/issues/5787
- **Status:** Open
- **Tier:** B
- **Framework:** Google ADK

## CFR Classification
- **Failure Mode:** C3 假信信号 — server reports startup, /dev-ui/ returns 404; WARNING buried in logs
- **Taxonomy:** configuration

## Details
Undeclared transitive deps (google-cloud-storage, python-multipart) cause DevServer import failure; fallback to ApiServer without dev UI
