---
cf_id: CF-0112
title: "custom provider config (apiKey/baseUrl) silently ignored at runtime"
category: Provider Configuration
affected_system: Hermes Agent (provider config)
layer: Configuration→Provider Registration
failure_mode: "C3 假信信号 — config claims provider configured with custom params, runtime uses defaults silently"
trigger: "User defines custom provider with apiKey/baseUrl in config.yaml"
observed_symptom: "Provider connects to wrong endpoint or uses wrong credentials; no config mismatch error"
root_cause: "Provider loader reads config but doesn't pass apiKey/baseUrl to internal provider instance"
detection: "Indirect: API errors (wrong endpoint/auth) that don't reference config; user must diff config vs actual behavior"
impact: "Custom providers silently dysfunctional; debugging confounded by config claiming correct values"
mitigation: "None at time; required code fix to wire config params"
reference: 
issue: https://github.com/NousResearch/hermes-agent/issues/8919
status: Closed
tier: B
taxonomy: configuration
old_cf_id: 
pr: https://github.com/NousResearch/hermes-agent/pull/50385
evidence_level: reproduced
date_added: 2026-07-21
---
# CF-0112: custom provider config (apiKey/baseUrl) silently ignored at runtime

## Source
- **Issue:** https://github.com/NousResearch/hermes-agent/issues/8919
- **Status:** Closed
- **Tier:** B
- **Framework:** Hermes Agent

## CFR Classification
- **Failure Mode:** C3 假信信号 — config claims provider configured with custom params, runtime uses defaults silently
- **Taxonomy:** configuration

## Details
Provider loader reads config but doesn't pass apiKey/baseUrl to internal provider instance
