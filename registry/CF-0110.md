---
cf_id: CF-0110
title: "Rate limit (429/timeout) failures silently drop user messages — #1630 regression"
category: Message Delivery
affected_system: Hermes Agent (gateway)
layer: Provider→Gateway
failure_mode: "C1 静默失效 + C2 跨边界 — message dropped at provider boundary, gateway acts as if never received"
trigger: "Primary provider returns 429 rate-limit or timeout"
observed_symptom: "User message disappears; no fallback try, no error response, session transcript broken"
root_cause: "Regression from #1630: error path in gateway message dispatch drops message instead of retrying/falling back"
detection: "Indirect: user sees no response, assumes agent ignored them"
impact: "Complete message loss; user has no way to know message was dropped vs queued"
mitigation: "None at time of report; required code fix"
reference: 
issue: https://github.com/NousResearch/hermes-agent/issues/7100
status: Closed
tier: B
taxonomy: communication
old_cf_id: 
pr: https://github.com/NousResearch/hermes-agent/pull/17900
evidence_level: reproduced
date_added: 2026-07-21
---
# CF-0110: Rate limit (429/timeout) failures silently drop user messages — #1630 regression

## Source
- **Issue:** https://github.com/NousResearch/hermes-agent/issues/7100
- **Status:** Closed
- **Tier:** B
- **Framework:** Hermes Agent

## CFR Classification
- **Failure Mode:** C1 静默失效 + C2 跨边界 — message dropped at provider boundary, gateway acts as if never received
- **Taxonomy:** communication

## Details
Regression from #1630: error path in gateway message dispatch drops message instead of retrying/falling back
