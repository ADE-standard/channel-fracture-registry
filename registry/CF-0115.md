---
cf_id: CF-0115
title: "State channels reference-transparent end-to-end — caller object corruption across invocations"
category: State Management
affected_system: LangGraph (state channels)
layer: Caller→Graph State
failure_mode: "C1 静默失效 + C2 跨边界 — state channels alias caller objects; mutations leak across .invoke() boundaries"
trigger: "User passes mutable list/dict as state, then mutates it between or after .invoke() calls"
observed_symptom: "State inside graph silently changes; subsequent runs behave differently with same inputs"
root_cause: "LastValue.update()/get() are direct reference assignments (no copy-on-write); first .invoke() makes channel live value = caller object"
detection: "Extremely indirect: non-deterministic behavior across runs, reproducible only by tracking object identity"
impact: "Silent state corruption across agent invocations; root cause takes deep forensic analysis to locate"
mitigation: "Deep-copy inputs before passing to .invoke(); aware users must defensively copy everything"
reference: 
issue: https://github.com/langchain-ai/langgraph/issues/8314
status: Open
tier: B
taxonomy: state
old_cf_id: 
pr: 
evidence_level: reproduced
date_added: 2026-07-21
---
# CF-0115: State channels reference-transparent end-to-end — caller object corruption across invocations

## Source
- **Issue:** https://github.com/langchain-ai/langgraph/issues/8314
- **Status:** Open
- **Tier:** B
- **Framework:** LangGraph

## CFR Classification
- **Failure Mode:** C1 静默失效 + C2 跨边界 — state channels alias caller objects; mutations leak across .invoke() boundaries
- **Taxonomy:** state

## Details
LastValue.update()/get() are direct reference assignments (no copy-on-write); first .invoke() makes channel live value = caller object
