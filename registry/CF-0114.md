---
cf_id: CF-0114
title: "extra_body in OpenAIChatCompletionClient silently ignored via AutoGen Studio JSON"
category: Model Configuration
affected_system: AutoGen (AutoGen Studio)
layer: Configuration→Model Client
failure_mode: "C3 假信信号 — config has extra_body, deserialization drops it; endpoint never receives params"
trigger: "User configures extra_body (e.g. enable_thinking:false) via Studio JSON editor"
observed_symptom: "Endpoint still receives requests with enable_thinking active despite config; BadRequestError surfaces"
root_cause: "AutoGen Studio JSON deserializer doesn't map extra_body field to OpenAIChatCompletionClient params"
detection: "Indirect: 400 error from model endpoint; user must diff Studio config vs actual HTTP request"
impact: "Model behavior not configurable via Studio UI; users believe params are active but model disagrees"
mitigation: "Use Python direct instantiation instead of Studio JSON"
reference: 
issue: https://github.com/microsoft/autogen/issues/7418
status: Open
tier: B
taxonomy: configuration
old_cf_id: 
pr: 
evidence_level: reproduced
date_added: 2026-07-21
---
# CF-0114: extra_body in OpenAIChatCompletionClient silently ignored via AutoGen Studio JSON

## Source
- **Issue:** https://github.com/microsoft/autogen/issues/7418
- **Status:** Open
- **Tier:** B
- **Framework:** AutoGen

## CFR Classification
- **Failure Mode:** C3 假信信号 — config has extra_body, deserialization drops it; endpoint never receives params
- **Taxonomy:** configuration

## Details
AutoGen Studio JSON deserializer doesn't map extra_body field to OpenAIChatCompletionClient params
